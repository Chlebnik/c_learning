#include <stdio.h>
#include <string.h>

void reverse_string(char *str) {
    // TODO: Implement string reversal using pointers
}

int main() {
    // TODO: Read a string from stdin
    // Call reverse_string(str)
    // Print the result
    return 0;
}
