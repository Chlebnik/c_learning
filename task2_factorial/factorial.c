#include <stdio.h>

long long factorial(int n) {
    // TODO: Implement the factorial function
    return 0;
}

int main() {
    // TODO: Read an integer from stdin
    // Call factorial(n)
    // Print the result
    return 0;
}
